all:
  vars:

  children:
    # Individual host groups

    canterlot:
      vars:
        ansible_host: "10.1.1.1"   # Domain Controller (AD)
      hosts:
        canterlot:

    manehatten:
      vars:
        ansible_host: "10.1.1.2"   # File Server (SMB)
      hosts:
        manehatten:

    ponyville:
      vars:
        ansible_host: "10.1.1.3"   # Internal Web (Debian)
      hosts:
        ponyville:

    seaddle:
      vars:
        ansible_host: "10.1.1.4"   # Email (Debian)
      hosts:
        seaddle:

    crystal_empire:
      vars:
        ansible_host: "10.1.1.5"   # Database (CachyOS)
      hosts:
        crystal-empire:

    baltamare:
      vars:
        ansible_host: "192.168.1.1"   # Win10 WKST1
      hosts:
        baltamare:

    neighara_falls:
      vars:
        ansible_host: "192.168.1.2"   # Win10 WKST2
      hosts:
        neighara-falls:

    cloudsdale:
      vars:
        ansible_host: "192.168.1.3"   # Ubuntu WKST3
      hosts:
        cloudsdale:

    vanhoover:
      vars:
        ansible_host: "192.168.1.4"   # Ubuntu WKST4
      hosts:
        vanhoover:

    fillydelphia:
      vars:
        ansible_host: "192.168.1.5"  # Windows XP Boss
      hosts:
        fillydelphia:

    las_pegasus:
      vars:
        ansible_host: "172.16.1.1"  # External Web (IIS)
      hosts:
        las-pegasus:

    appleloosa:
      vars:
        ansible_host: "172.16.1.2"  # External DB (MSSQL)
      hosts:
        appleloosa:

    everfree_forest:
      vars:
        ansible_host: "172.16.1.3"  # IRC Chat
      hosts:
        everfree-forest:

    griffonstone:
      vars:
        ansible_host: "172.16.1.4"  # External FTP
      hosts:
        griffonstone:


# OS-based groups
    # Hosts that are Windows
    windows:
      children:
        canterlot:
        manehatten:
        baltamare:
        neighara_falls:
        fillydelphia:
        las_pegasus:
        appleloosa:
    # Hosts that are Linux
    linux:
      children:
        ponyville:
        seaddle:
        crystal_empire:
        cloudsdale:
        vanhoover:
        everfree_forest:
        griffonstone:
    unix:
      children:
        linux:


    # Hosts for the Teams
    inscope:
      children:
        manehatten:
        ponyville:
        seaddle:
        crystal_empire:
        baltamare:
        neighara_falls:
        cloudsdale:
        vanhoover:
        fillydelphia:
        las_pegasus:
        appleloosa:
        everfree_forest:
        griffonstone:
    # Hosts for management/SIEMs
    mgmt:
      children:
        canterlot:
